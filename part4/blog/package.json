{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Christopher Miller",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=prod node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "test-only": "cross-env NODE_ENV=test node --test --test-only",
    "test-log": "cross-env NODE_ENV=test node --test --test-reporter=spec"
  },
  "dependencies": {
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "lodash": "^4.17.21",
    "mongoose": "^8.9.2",
    "supertest": "^7.1.4"
  }
}